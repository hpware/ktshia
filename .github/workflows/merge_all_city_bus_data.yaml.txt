name: Merge all City Bus Data for search indexing

on:
  push:
    branches:
      - main
    paths:
      - "data/**"
  workflow_dispatch:

jobs:
  merge_data:
    name: Merge Data
    runs-on: ubuntu-latest
    environment: updatedata
    defaults:
      run:
        working-directory: backend-service/app
